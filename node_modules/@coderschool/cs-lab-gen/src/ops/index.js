const resolve = (attributes) => {
  if (attributes.to && !attributes.inject) return require("./add");

  if (attributes.to && attributes.inject) return require("./inject");

  if (attributes.sh) return require("./shell");
};

module.exports = resolve;
